{
  "buildCommand": "cd Backend && npm install && npm run build",
  "installCommand": "",
  "framework": null,
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "Backend/api/index.ts"
    }
  ]
}